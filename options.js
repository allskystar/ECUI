var FeatureFlags = {
    SINGLETON_1: true,    // 保证同一个类只有唯一的单例，如果不存在则创建，存在则返回
    SYSTEM_PATCH_1: true,  // 支持系统增量补丁
    CACHE_1: true,         // 框架缓存功能
    INERTIA_1: true,       // 内核增加惯性处理的功能
    PAGEON_1: true         // 分页组件耦合分页处理
};
